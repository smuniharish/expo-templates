import fontSizes from "./fontSizes";
import iconSizes from "./iconSizes";

// here we are not importing the logsConfig. logsConfig having multiple exports and ignore these kind of exports
export {fontSizes,iconSizes}